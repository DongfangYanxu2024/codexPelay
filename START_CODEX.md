# START CODEX

请维护并精进当前 Code++ v0.3 项目；`docs/CODEPP_SPEC.md` 保留第一阶段设计背景，当前行为以根目录 `AGENTS.md`、README 和 CLI/Security 文档为准。

先阅读根目录 `AGENTS.md` 和完整规格书，然后直接开始实施。不要只给我方案，也不要停在项目骨架；请完成代码、测试、CLI 实际验证和文档。

不要实现浏览器自动化、非公开 Web API、自动执行 Planner 命令、云后端、GUI 或 VS Code Extension。新功能必须保持 Human-in-the-loop、最小上下文和本地执行边界。

遇到不影响核心架构的小型实现选择时自行采用最简单、安全、跨平台且易维护的方案，不需要反复询问我。

完成后运行完整测试和一次端到端临时 Git 仓库验证，再向我汇报最终结果。
